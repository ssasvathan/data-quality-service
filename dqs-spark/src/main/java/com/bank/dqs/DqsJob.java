package com.bank.dqs;

/**
 * DqsJob — placeholder entry point for the Data Quality Spark job.
 *
 * <p>Receives a parent path and optional dataset filter via CLI arguments,
 * runs configured DQ checks, and writes results to Postgres.
 */
public class DqsJob {

    public static void main(String[] args) {
        // TODO: parse args, build SparkSession, run checks, write results
        System.out.println("DQS Spark Job — not yet implemented");
    }
}
